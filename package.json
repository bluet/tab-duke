{
  "name": "tab-duke",
  "description": "Professional tab manager with lightning-fast search, bulk operations, and keyboard shortcuts. Built for power users managing extreme tab loads with production-grade reliability and comprehensive testing.",
  "version": "1.2.0",
  "type": "module",
  "devDependencies": {
    "@babel/core": "^7.28.5",
    "@babel/eslint-parser": "^7.23.3",
    "@babel/preset-env": "^7.28.5",
    "babel-jest": "^30.2.0",
    "eslint": "^8.55.0",
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0",
    "prettier-eslint": "^16.1.2",
    "puppeteer": "^24.33.0",
    "tailwindcss": "^3.4.19"
  },
  "scripts": {
    "test": "jest --testPathIgnorePatterns='integration/(extension-loads|real-tab-switching|search-real-data)'",
    "test:integration": "jest --testPathPatterns='integration/(extension-loads|real-tab-switching|search-real-data)'",
    "test:integration:safe": "echo '⚠️  Chrome integration tests require manual opt-in due to browser launch risks in CI environments.' && echo 'Run: npm run test:chrome:opt-in'",
    "test:chrome:opt-in": "echo '🚀 Starting Chrome browser tests (requires display and Chrome installation)...' && jest --testPathPatterns='integration/(extension-loads|real-tab-switching|search-real-data)'",
    "test:all": "npm run test",
    "test:all:with-chrome": "npm run test && npm run test:chrome:opt-in",
    "test:watch": "jest --watch --testPathIgnorePatterns='integration/(extension-loads|real-tab-switching|search-real-data)'",
    "test:coverage": "jest --coverage --testPathIgnorePatterns='integration/(extension-loads|real-tab-switching|search-real-data)'",
    "test:coverage:all": "jest --coverage",
    "build-css": "npx tailwindcss -i ./src/tailwind.css -o ./dist/tailwind.min.css --minify",
    "watch-css": "npx tailwindcss -i ./src/tailwind.css -o ./dist/tailwind.min.css --watch",
    "pack": "npm run build-css && bash ./scripts/pack-extension.sh"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/bluet/tab-duke.git"
  },
  "keywords": [
    "tab",
    "management",
    "chrome"
  ],
  "author": "BlueT - Matthew Lien <bluet@bluet.org>",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/bluet/tab-duke/issues"
  },
  "homepage": "https://github.com/bluet/tab-duke#readme",
  "dependencies": {
    "baseline-browser-mapping": "^2.9.7",
    "caniuse-lite": "^1.0.30001760"
  }
}
